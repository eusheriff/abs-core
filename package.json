{
  "name": "oconnector-abs-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "lint": "eslint packages/*/src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vitest": "^0.34.0",
    "tsx": "^4.7.0",
    "eslint": "^8.0.0"
  }
}
