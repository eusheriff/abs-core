{
  "name": "oconnector-abs-monorepo",
  "version": "0.5.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:e2e": "vitest run packages/core/test/e2e.test.ts",
    "lint": "eslint packages/*/src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vitest": "^0.34.0",
    "tsx": "^4.7.0",
    "eslint": "^8.0.0"
  }
}
