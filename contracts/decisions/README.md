# Contracts: Decisions

Este diretório contém os contratos que definem os tipos de decisão suportados pelo ABS Core para cada domínio.

## Estrutura

- Cada arquivo define um set de `ActionTypes` permitidos.
- Define schemas para `action_params` (quais dados a IA deve fornecer).
